-- plan to use the where clause to create a private scope for functions
def Secrets {
    public_function() : String = secret_value(secret_key)
} where {
    secret_value(key : String) : String = "shh, it's " ++ key,
    secret_key = "baby"
}

let result = Secrets::public_function(); -- compiles

let key = Secrets::secret_key; -- does not compile