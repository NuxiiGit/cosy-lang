-- directives start with `#`
-- I have not thought of many good ones, but the idea is for them to be valid expressions
let line_no = #line;

#def DEBUG_ENABLE;
let title = #ifdef DEBUG_ENABLE "debug mode" "release mode";
#undef DEBUG_ENABLE;

#const SCALE 2;
if SCALE > 1 {
	print "big boy";
}